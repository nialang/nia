using std::build;

fn check(b: &mut build::Build) build::Error!void { _ = b; !{} }

pub fn build(b: &mut build::Build) build::Error!void {
    static checkName = "check";
    _ = b.addStep(&checkName, &check).?;
    !{}
}
