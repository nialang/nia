using entry::math;
using entry::palette;

// A facade module can re-export modules, selected items, renamed items, and enum
// variants so users do not need to know the internal file layout.
pub using math;
pub using math::{Point, add};
pub using palette::Color;
pub using palette::Color::*;
pub using palette::code as color_code;
