pub enum Color: u8 {
    Red,
    Black,
    Green,
}

pub fn code(color: Color) i32 {
    // match over an enum must cover every variant unless a wildcard is used.
    match color {
        Color::Red => 1,
        Color::Black => 2,
        Color::Green => 3,
    }
}
