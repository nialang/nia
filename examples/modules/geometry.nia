pub struct Point {
    x: i32,
    y: i32,
}

pub fn add(a: i32, b: i32) i32 {
    a + b
}

pub fn sub(a: i32, b: i32) i32 {
    a - b
}

extend Point {
    pub fn len2(&self) i32 {
        self.x * self.x + self.y * self.y
    }
}
