using helper;
using std::process;

pub fn main(init: process::Init) process::ExitCode!() {
    _ = init;
    let value = helper::value();
    if value + 1 != 8 {
        return process::exit(1)!;
    }
    !()
}
