pub enum Ordering: i32 {
    Less = 1,
    Equal = 2,
    Greater = 3,
}

// Return the greater value, preserving `left` when the values compare equal.
pub fn max[T](left: T, right: T) T
where T: Sized + Ord[T]
{
    if right > left {
        right
    } else {
        left
    }
}
