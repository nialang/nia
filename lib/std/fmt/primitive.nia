using pkg::fmt::core;

fn formatSignedSpec(
    value: i128,
    formatter: &mut core::Formatter,
    spec: core::FormatSpec,
) core::Error!() {
    if spec.hasPrecision() {
        return core::Error::InvalidTemplate!;
    }
    match spec.presentation() {
        .Display => formatter.writeSignedRadixSpec(value, 10, false, spec),
        .LowerHex => formatter.writeSignedRadixSpec(value, 16, false, spec),
        .UpperHex => formatter.writeSignedRadixSpec(value, 16, true, spec),
        .Binary => formatter.writeSignedRadixSpec(value, 2, false, spec),
        .Octal => formatter.writeSignedRadixSpec(value, 8, false, spec),
        _ => core::Error::InvalidTemplate!,
    }
}

fn formatUnsignedSpec(
    value: u128,
    formatter: &mut core::Formatter,
    spec: core::FormatSpec,
) core::Error!() {
    if spec.signPlus() or spec.hasPrecision() {
        return core::Error::InvalidTemplate!;
    }
    match spec.presentation() {
        .Display => formatter.writeUnsignedRadixSpec(value, 10, false, spec),
        .LowerHex => formatter.writeUnsignedRadixSpec(value, 16, false, spec),
        .UpperHex => formatter.writeUnsignedRadixSpec(value, 16, true, spec),
        .Binary => formatter.writeUnsignedRadixSpec(value, 2, false, spec),
        .Octal => formatter.writeUnsignedRadixSpec(value, 8, false, spec),
        _ => core::Error::InvalidTemplate!,
    }
}

fn formatTextSpec(
    formatter: &mut core::Formatter,
    text: &[char],
    spec: core::FormatSpec,
) core::Error!() {
    if spec.presentation() != core::FormatPresentation::Display or spec.signPlus() or spec.alternate() {
        return core::Error::InvalidTemplate!;
    }
    let contentWidth = formatter.precisionWidth(text.len(), spec);
    formatter.writePaddingBefore(contentWidth, spec).?;
    formatter.writeTextPrefix(text, contentWidth).?;
    formatter.writePaddingAfter(contentWidth, spec)
}

fn formatBytesSpec(
    formatter: &mut core::Formatter,
    bytes: &[u8],
    spec: core::FormatSpec,
) core::Error!() {
    if spec.presentation() != core::FormatPresentation::Display or spec.signPlus() or spec.alternate() {
        return core::Error::InvalidTemplate!;
    }
    let contentWidth = formatter.precisionWidth(bytes.len(), spec);
    formatter.writePaddingBefore(contentWidth, spec).?;
    formatter.writeBytes(&bytes[0..contentWidth]).?;
    formatter.writePaddingAfter(contentWidth, spec)
}

extend i8 : core::Format {
    fn format(&self, formatter: &mut core::Formatter) core::Error!() {
        formatter.writeSigned(self.* as i128)
    }

    fn formatSpec(&self, formatter: &mut core::Formatter, spec: core::FormatSpec) core::Error!() {
        formatSignedSpec(self.* as i128, formatter, spec)
    }
}

extend i16 : core::Format {
    fn format(&self, formatter: &mut core::Formatter) core::Error!() {
        formatter.writeSigned(self.* as i128)
    }

    fn formatSpec(&self, formatter: &mut core::Formatter, spec: core::FormatSpec) core::Error!() {
        formatSignedSpec(self.* as i128, formatter, spec)
    }
}

extend i32 : core::Format {
    fn format(&self, formatter: &mut core::Formatter) core::Error!() {
        formatter.writeSigned(self.* as i128)
    }

    fn formatSpec(&self, formatter: &mut core::Formatter, spec: core::FormatSpec) core::Error!() {
        formatSignedSpec(self.* as i128, formatter, spec)
    }
}

extend i64 : core::Format {
    fn format(&self, formatter: &mut core::Formatter) core::Error!() {
        formatter.writeSigned(self.* as i128)
    }

    fn formatSpec(&self, formatter: &mut core::Formatter, spec: core::FormatSpec) core::Error!() {
        formatSignedSpec(self.* as i128, formatter, spec)
    }
}

extend i128 : core::Format {
    fn format(&self, formatter: &mut core::Formatter) core::Error!() {
        formatter.writeSigned(self.*)
    }

    fn formatSpec(&self, formatter: &mut core::Formatter, spec: core::FormatSpec) core::Error!() {
        formatSignedSpec(self.*, formatter, spec)
    }
}

extend isize : core::Format {
    fn format(&self, formatter: &mut core::Formatter) core::Error!() {
        formatter.writeSigned(self.* as i128)
    }

    fn formatSpec(&self, formatter: &mut core::Formatter, spec: core::FormatSpec) core::Error!() {
        formatSignedSpec(self.* as i128, formatter, spec)
    }
}

extend u8 : core::Format {
    fn format(&self, formatter: &mut core::Formatter) core::Error!() {
        formatter.writeUnsigned(self.* as u128)
    }

    fn formatSpec(&self, formatter: &mut core::Formatter, spec: core::FormatSpec) core::Error!() {
        formatUnsignedSpec(self.* as u128, formatter, spec)
    }
}

extend u16 : core::Format {
    fn format(&self, formatter: &mut core::Formatter) core::Error!() {
        formatter.writeUnsigned(self.* as u128)
    }

    fn formatSpec(&self, formatter: &mut core::Formatter, spec: core::FormatSpec) core::Error!() {
        formatUnsignedSpec(self.* as u128, formatter, spec)
    }
}

extend u32 : core::Format {
    fn format(&self, formatter: &mut core::Formatter) core::Error!() {
        formatter.writeUnsigned(self.* as u128)
    }

    fn formatSpec(&self, formatter: &mut core::Formatter, spec: core::FormatSpec) core::Error!() {
        formatUnsignedSpec(self.* as u128, formatter, spec)
    }
}

extend u64 : core::Format {
    fn format(&self, formatter: &mut core::Formatter) core::Error!() {
        formatter.writeUnsigned(self.* as u128)
    }

    fn formatSpec(&self, formatter: &mut core::Formatter, spec: core::FormatSpec) core::Error!() {
        formatUnsignedSpec(self.* as u128, formatter, spec)
    }
}

extend u128 : core::Format {
    fn format(&self, formatter: &mut core::Formatter) core::Error!() {
        formatter.writeUnsigned(self.*)
    }

    fn formatSpec(&self, formatter: &mut core::Formatter, spec: core::FormatSpec) core::Error!() {
        formatUnsignedSpec(self.*, formatter, spec)
    }
}

extend usize : core::Format {
    fn format(&self, formatter: &mut core::Formatter) core::Error!() {
        formatter.writeUnsigned(self.* as u128)
    }

    fn formatSpec(&self, formatter: &mut core::Formatter, spec: core::FormatSpec) core::Error!() {
        formatUnsignedSpec(self.* as u128, formatter, spec)
    }

    fn formatUsizeArg(&self) ?usize {
        ?self.*
    }
}

fn formatPointerSpec(
    value: usize,
    formatter: &mut core::Formatter,
    spec: core::FormatSpec,
) core::Error!() {
    if spec.presentation() == core::FormatPresentation::Display or spec.presentation() == core::FormatPresentation::Pointer {
        formatter.writePointerSpec(value, spec)
    } else {
        core::Error::InvalidTemplate!
    }
}

extend[T] &T : core::Format
where T: Sized
{
    fn format(&self, formatter: &mut core::Formatter) core::Error!() {
        formatter.writePointer(self.* as usize)
    }

    fn formatSpec(&self, formatter: &mut core::Formatter, spec: core::FormatSpec) core::Error!() {
        formatPointerSpec(self.* as usize, formatter, spec)
    }
}

extend[T] &mut T : core::Format
where T: Sized
{
    fn format(&self, formatter: &mut core::Formatter) core::Error!() {
        formatter.writePointer(self.* as usize)
    }

    fn formatSpec(&self, formatter: &mut core::Formatter, spec: core::FormatSpec) core::Error!() {
        formatPointerSpec(self.* as usize, formatter, spec)
    }
}

extend bool : core::Format {
    fn format(&self, formatter: &mut core::Formatter) core::Error!() {
        if self.* {
            formatter.writeText(&"true")
        } else {
            formatter.writeText(&"false")
        }
    }

    fn formatSpec(&self, formatter: &mut core::Formatter, spec: core::FormatSpec) core::Error!() {
        if self.* {
            formatTextSpec(formatter, &"true", spec)
        } else {
            formatTextSpec(formatter, &"false", spec)
        }
    }
}

extend char : core::Format {
    fn format(&self, formatter: &mut core::Formatter) core::Error!() {
        formatter.writeChar(self.*)
    }

    fn formatSpec(&self, formatter: &mut core::Formatter, spec: core::FormatSpec) core::Error!() {
        if spec.presentation() != core::FormatPresentation::Display or spec.signPlus() or spec.alternate() {
            return core::Error::InvalidTemplate!;
        }
        let contentWidth = formatter.precisionWidth(1, spec);
        formatter.writePaddingBefore(contentWidth, spec).?;
        if contentWidth != 0 {
            formatter.writeChar(self.*).?;
        }
        formatter.writePaddingAfter(contentWidth, spec)
    }
}

extend [char] : core::Format {
    fn format(&self, formatter: &mut core::Formatter) core::Error!() {
        let mut index: usize = 0;
        while index < self.len() {
            formatter.writeChar(self[index]).?;
            index += 1;
        }
        !()
    }

    fn formatSpec(&self, formatter: &mut core::Formatter, spec: core::FormatSpec) core::Error!() {
        formatTextSpec(formatter, self, spec)
    }
}

extend [u8] : core::Format {
    fn format(&self, formatter: &mut core::Formatter) core::Error!() {
        formatter.writeBytes(self)
    }

    fn formatSpec(&self, formatter: &mut core::Formatter, spec: core::FormatSpec) core::Error!() {
        formatBytesSpec(formatter, self, spec)
    }
}

extend[T] [T] : core::Format
where T: Sized + core::Format
{
    fn format(&self, formatter: &mut core::Formatter) core::Error!() {
        formatter.writeByte(b'[').?;
        let mut index: usize = 0;
        while index < self.len() {
            if index != 0 {
                formatter.writeText(&", ").?;
            }
            self[index].format(formatter).?;
            index += 1;
        }
        formatter.writeByte(b']')
    }

    fn formatSpec(&self, formatter: &mut core::Formatter, spec: core::FormatSpec) core::Error!() {
        spec.validate().?;
        if spec.presentation() != core::FormatPresentation::Display or spec.width() != 0 or spec.hasPrecision() or spec.signPlus() or spec.alternate() {
            return core::Error::InvalidTemplate!;
        }
        self.format(formatter)
    }
}
