using pkg::process::types;

using types::{Args, Env, Init};

extend Init {
    pub(pkg) fn init(argc: usize, argv: &&u8, envp: &&u8) Init {
        .{ args: .init(argc, argv), env: .init(envp) }
    }

    pub fn argc(&self) usize {
        self.args.len()
    }

    pub fn args(&self) Args {
        self.args
    }

    pub fn env(&self) Env {
        self.env
    }
}
