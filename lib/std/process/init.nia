using pkg::process::types;

using types::{Args, Env, Init};

extend Init {
    // Runtime startup constructs the public process context before invoking
    // the language-level entry::main contract.
    pub fn init(argc: usize, argv: &&u8, envp: &&u8) Init {
        .{ args: .init(argc, argv), env: .init(envp) }
    }

    pub fn argc(&self) usize {
        self.args.len()
    }

    pub fn args(&self) Args {
        self.args
    }

    pub fn env(&self) Env {
        self.env
    }
}
