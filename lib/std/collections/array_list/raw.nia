using pkg::mem;

pub fn danglingSlice[T](len: usize) &mut [T]
where T: Sized
{
    let mut ptr = std::builtin::align[T]() as &mut T;
    &mut ptr[0..len]
}

pub fn blockItems[T](block: mem::Block, len: usize) &mut [T]
where T: Sized
{
    let mut ptr = block.ptr() as &mut T;
    &mut ptr[0..len]
}
