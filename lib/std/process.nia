pub(pkg) module args;
pub(pkg) module child;
pub(pkg) module command;
pub(pkg) module env;
pub(pkg) module exit;
pub(pkg) module init;
pub(pkg) module term;
pub(pkg) module types;

pub using command::{run, spawn, spawn_raw, spawn_with_options};
pub using exit::exit;
pub using types::{
    Arg,
    Args,
    ArgsIter,
    Child,
    Command,
    Env,
    EnvIter,
    EnvVar,
    ExitCode,
    Init,
    StdIo,
    Term,
    TermKind,
};
