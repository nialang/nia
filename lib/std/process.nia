// Process startup views, commands, child handles, environment, and exit codes.
//
// Command configuration is borrowed until spawn lowers it into invocation-owned
// native storage. Child streams and handles require explicit close or wait
// operations, and spawn failures retain their stage and underlying cause.

pub(pkg) module args;
pub(pkg) module child;
pub(pkg) module command;
pub(pkg) module env;
pub(pkg) module exit;
pub(pkg) module init;
pub(pkg) module pipe;
pub(pkg) module term;
pub(pkg) module types;

using pkg::process::args;
using pkg::process::child;
using pkg::process::command;
using pkg::process::env;
using pkg::process::exit;
using pkg::process::init;
using pkg::process::pipe;
using pkg::process::term;

pub using exit::exit;
pub using types::{
    Arg,
    Args,
    ArgsIter,
    Child,
    ChildStderr,
    ChildStdin,
    ChildStdout,
    Command,
    Env,
    EnvEntry,
    EnvEntryError,
    EnvIter,
    EnvVar,
    Error,
    ExitCode,
    Init,
    ProcessId,
    Signal,
    SpawnError,
    StdIo,
    StdStream,
    SystemError,
    Term,
    TermKind,
};
