// Bit-counting compiler intrinsics.

// Counts trailing zero bits.
@[builtin("ctz")]
pub fn ctz[T](value: T) T;

// Counts leading zero bits.
@[builtin("clz")]
pub fn clz[T](value: T) T;

// Counts one bits.
@[builtin("popcount")]
pub fn popcount[T](value: T) T;
