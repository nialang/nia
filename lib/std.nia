// Nia standard library root.
//
// Public standard-library surfaces are re-exported from here so users can
// import `std` as the stable facade.

import std.io;
import std.fmt;
import std.mem;
import std.os;
import std.process;
import std.unicode;

pub using {fmt, io, mem, os, process, unicode};
